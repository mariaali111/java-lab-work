# Week 13 – File Handling in JAVA

This week’s lab focuses on performing basic **file handling operations** in Java. The exercises include creating files, writing to files, copying files, and analyzing file contents such as total characters and total lines.

---

## ✅ Topics Covered

- Creating new files using Java I/O  
- Writing data into files  
- Copying contents from one file to another  
- Reading files using `FileReader`, `BufferedReader`, and related classes  
- Counting characters in a file  
- Counting lines in a file  
- Working with Java I/O streams

---

## 📁 Files

- **Problem01.java** →  
  A Java program to **create a new file** using classes such as `File` or `FileOutputStream`.

- **Problem02.java** →  
  A program to **write data into a file** using `FileWriter`, `BufferedWriter`, or `FileOutputStream`.

- **Problem03.java** →  
  A program to **copy contents of one file into another file** using input and output streams.

- **Problem04.java** →  
  Reads a file and **counts the total number of characters** present in it.

- **Problem05.java** →  
  Reads a file and **counts the total number of lines** it contains.
